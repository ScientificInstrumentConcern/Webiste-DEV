import { Typography } from '@material-ui/core';
import React from 'react';

function Footer() {
    return (
        <div style={{ padding: '1rem 0' }}>
            <Typography align="center">Admin Pannel of SINCON</Typography>
        </div>
    );
}

export default Footer;
